const flexColumnCenter = {
  display: "flex",
  flexDirection: "column",
  alignItems: "center"
};

export default theme => ({
  root: {
    ...flexColumnCenter
  },
  section: {
    ...flexColumnCenter
  }
});
